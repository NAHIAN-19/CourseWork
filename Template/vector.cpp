// Sum: int sum = accumulate(v.begin(), v.end(), 0);
// Find: auto it = find(v.begin(), v.end(), value);
// Count: int count = count(v.begin(), v.end(), value);
// Insert: v.insert(v.begin() + 2, value);
// Erase: v.pop_back(); v.erase(v.begin() + 2);
// Rotate: rotate(arr.begin(), arr.begin() + givenindex, arr.end()); // Rotate 'arr' by 'k' positions to the left
// Count if: int cnt = count_if(arr.begin(), arr.end(), [](int x){ return x % 2 == 0; });

// Unique vector: unique(v.begin(), v.end());
// Unique string: unique(str.begin(), str.end());
// Next permutation: next_permutation(v.begin(), v.end());
// Previous permutation: prev_permutation(v.begin(), v.end());
// Get max element index: int maxElementIndex = max_element(all(v)) - v.begin();
// Get min element index: int minElementIndex = min_element(all(v)) - v.begin();
