///////////////// ----- String -----////////////////////////
// Find substring: size_t foundIndex = str.find(substring);
// Replace substring: str.replace(startIndex, length, newSubstring);
// Get substring: string sub = str.substr(startIndex, length);
// Convert to int: int num = stoi(str);
// Convert to long long: long long num = stoll(str);
// Convert to double: double num = stod(str);
// Check if alpha: bool isAlpha = isalpha(ch);
// Check if digit: bool isDigit = isdigit(ch);
// Convert to uppercase: char upperCh = toupper(ch);
// Convert to lowercase: char lowerCh = tolower(ch);
// Remove leading and trailing whitespaces: str.erase(0, str.find_first_not_of(' ')); str.erase(str.find_last_not_of(' ') + 1);
// Remove all whitespaces: str.erase(remove(str.begin(), str.end(), ' '), str.end());
