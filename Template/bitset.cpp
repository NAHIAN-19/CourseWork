///////////////// ----- Bitset -----////////////////////////
// Bitset: bitset<10> b;
// Set bit: b.set(index);
// Clear bit: b.reset(index);
// Flip bit: b.flip(index);
// Check if bit is set: bool isSet = b.test(index);
// Get number of set bits: int numSetBits = b.count();
// Get number of bits: int numBits = b.size();
// Convert to string: string str = b.to_string();
// Convert to ulong: ulong num = b.to_ulong();
